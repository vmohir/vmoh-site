{
  "name": "vahid-slides",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.15.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "pnpm -r --workspace-concurrency=1 build"
  },
  "devDependencies": {
    "oxlint": "^1.74.0",
    "prettier": "^3.9.5",
    "prettier-plugin-slidev": "^1.0.5"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  }
}
