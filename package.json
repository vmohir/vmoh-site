{
  "name": "vmoh-site",
  "version": "1.0.0",
  "description": "Personal site for Vahid Mohammadi",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "netlify-cli": "^23.14.0"
  }
}
