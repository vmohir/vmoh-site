[build]
  base = "."
  command = "pnpm run build"
  publish = "dist"
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- packages/homepage packages/old-homepage package.json pnpm-workspace.yaml netlify.toml"
